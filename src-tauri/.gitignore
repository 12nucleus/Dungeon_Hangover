# Generated by Cargo
# will have compiled files and executables
/target/*
!/target/release/
/target/release/*
!/target/release/bundle/
/target/release/bundle/*
!/target/release/bundle/nsis/
/target/release/bundle/nsis/*
!/target/release/bundle/nsis/*.exe
/gen/schemas

# Deliberately tracked: the Windows installer bundle (28 MB) is committed so
# the release can be pulled from the repo without a Rust toolchain.
